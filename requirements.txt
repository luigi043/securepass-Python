# Core — all stdlib (no install needed for core features)
# secrets, string, csv, json, hashlib, argparse, logging, datetime — all built-in

# Encryption (required)
cryptography>=42.0.0

# Clipboard support (optional — install separately if needed)
# pyperclip>=1.9.0

# QR code generation (optional — Phase 4)
# qrcode[pil]>=7.4.2

# Dashboard (optional — Phase 4)
# streamlit>=1.35.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
